# Sample Local Note

Hermes is being evaluated as a local Mac mini operator for the Helio command center.

The validation goal is intentionally narrow: confirm that Hermes can inspect local sandbox files and produce local summaries without connecting to Google Workspace, Supabase, Home Assistant, Helio dispatch, or the agent bus.

The expected result is a short summary that captures three facts:

- Hermes is installed as a local client only.
- External integrations remain disabled.
- Live bus reads and writes remain frozen until credential rotation is confirmed or explicitly deferred.

This note is synthetic sandbox data. It contains no credentials, private account data, or live integration targets.
